## What this PR does

<!-- One paragraph. The "why" matters more than the "what" for code review. -->

## How to test

<!-- Concrete steps a reviewer can run to verify. e.g.:
1. Build with `dotnet run --project src/ByesLauncher`
2. Search for "epoch" in the server list, click a result
3. Confirm Join button is enabled and modpack name is visible -->

## Checklist

- [ ] Builds cleanly (`dotnet build src/ByesLauncher` — zero warnings)
- [ ] Manually smoke-tested the change end-to-end on Windows
- [ ] No `dist/` or `.user` files committed
- [ ] No secrets, EC2 IPs, or admin URLs added to source/comments
